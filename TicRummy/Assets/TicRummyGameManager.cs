using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TicRummyGameManager : MonoBehaviour
{

    public bool NeedToDiscardOnWin = false;
    public bool CanOnlyBuyOnTurn = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
